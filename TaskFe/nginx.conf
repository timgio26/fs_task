server {
  listen 80;
  server_name localhost;

  root /usr/share/nginx/html;
  index index.html;

  location / {
    try_files $uri /index.html;
  }
  location /api/ {
    # proxy_pass http://host.docker.internal:8080/api/Task/;
    proxy_pass http://backend:8080/api/Task/;
  }
}